{
    "builds": [{
        "src": "bookMyShow/bookMyShow/wsgi.py",
        "u{
    "builds": [{
        "src": "bookMyShow/bookMyShow/wsgi.py",
        "use": "@vercel/python",
        "config": { 
            "maxLambdaSize": "15mb", 
            "runtime": "python3.11",
            "requirements": "requirements.txt"
        }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "bookMyShow/bookMyShow/wsgi.py"
        }
    ]
}se": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.11" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "bookMyShow/bookMyShow/wsgi.py"
        }
    ]
}